import React from 'react';

const Tetris = ({rows, columns}) => { // Game의 값을 매개변수로 받아옴
    return (
        <div>
            {/* 게임 시작 버튼 클릭하면 화면 게임 화면으로 전환 */}
        </div>
    );
}

export default Tetris;